TODO:
- ft_printf/:
  - Maybe you don't need to use this function in your project.
- ft_3d_point_calc.c, ft_set_window.c, ft_get_data.c:
  - These files are trying to create window in custom size and write in custom size but it is not working.